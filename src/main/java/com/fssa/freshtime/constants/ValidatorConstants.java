package com.fssa.freshtime.constants;

/**
 * Constants for defining length constraints used in validation.
 */
public class ValidatorConstants {
    /** Minimum length for very short values. */
    public static final int VERY_SHORT_MIN_LEN = 3;

    /** Minimum length for short values. */
    public static final int SHORT_MIN_LEN = 10;

    /** Maximum length for short values. */
    public static final int SHORT_MAX_LEN = 50;

    /** Maximum length for very long values. */
    public static final int VERY_LONG_MAX_LEN = 150;

    public static final int MIN_PASSWORD_LEN = 8;
}

